<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Add Product</title>
    <style>
      body {
        background-color: #252a2f; /* Dark green background */
        color: #fff; /* White text for contrast */
        font-family: sans-serif;
        padding: 20px;
      }

      h1 {
        color: #2ecc71; /* Green header text */
        text-align: center;
        margin-bottom: 20px;
      }

      form {
        background-color: #343a40; /* Darker form background */
        padding: 30px;
        border-radius: 5px;
      }

      label {
        display: block;
        margin-bottom: 10px;
      }

      input[type="text"],
      input[type="number"],
      input[type="file"] {
        width: 1300px;
        padding: 10px;
        border: 1px solid #343a40; /* Darker border */
        border-radius: 5px;
        background-color: #4a555e; /* Darker input background */
        color: #fff;
        margin-bottom: 10px;
      }

      input[type="text"]:focus,
      input[type="number"]:focus,
      input[type="file"]:focus {
        outline: none;
        border-color: #2ecc71; /* Green focus border */
      }

      input[type="submit"] {
        background-color: #2ecc71; /* Green submit button */
        color: #fff;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
      }

      input[type="submit"]:hover {
        background-color: #39b54a; /* Darker green on hover */
      }
    </style>
  </head>
  <body>
    <h1>Add Product</h1>
    <form
      action="/admin/add-product"
      method="POST"
      enctype="multipart/form-data"
    >
      <label for="productName">Product Name:</label>
      <input type="text" id="productName" name="productName" required />
      <br />
      <label for="price">Price:</label>
      <input type="number" id="productPrice" name="productPrice" required />
      <br />
      <label for="productImage">Product Image:</label>
      <input
        type="file"
        id="productImage"
        name="productImage"
        accept="image/*"
        required
      />
      <br />
      <input type="submit" value="Add Product" />
    </form>
  </body>
</html>
