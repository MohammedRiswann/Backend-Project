<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Update Product</title>
    <style>
      body {
        background-color: #f0fff0; /* Soft green background */
        font-family: sans-serif;
        font-size: 16px;
      }

      h1 {
        color: #27ae60; /* Vibrant green heading */
        text-align: center;
        font-size: 2rem;
        margin-top: 20px;
        margin-bottom: 30px;
      }

      form {
        border: 2px solid #2ecc71; /* Green border */
        padding: 20px;
        border-radius: 10px; /* Rounded corners */
        width: 400px;
        margin: 0 auto;
      }

      label {
        display: block;
        margin-bottom: 5px;
      }

      input[type="text"],
      input[type="number"],
      input[type="file"] {
        width: 370px;
        padding: 10px;
        border: 1px solid #27ae60; /* Green input borders */
        border-radius: 5px;
        margin-bottom: 10px;
      }

      input[type="submit"] {
        background-color: #2ecc71; /* Green button */
        color: white;
        padding: 12px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
      }

      input[type="submit"]:hover {
        background-color: #27ae60; /* Darker green on hover */
      }
    </style>
  </head>
  <body>
    <h1>Update Product</h1>
    <form
      action="/admin/updatedProduct/<%= id._id %>"
      method="POST"
      enctype="multipart/form-data"
    >
      <label for="productName">Product Name:</label>
      <input
        type="text"
        id="productName"
        name="productName"
        value="<%= id.name %>"
        required
      />
      <br /><br />
      <label for="productImage">Product Image:</label>
      <input
        type="file"
        id="productImage"
        name="productImage"
        accept="image/*"
        required
      />
      <br /><br />
      <label for="productPrice">Product Price:</label>
      <input
        type="number"
        id="productPrice"
        name="productPrice"
        value="<%= id.price %>"
        required
      />
      <br /><br />
      <input type="submit" value="Update Product" />
    </form>
  </body>
</html>
